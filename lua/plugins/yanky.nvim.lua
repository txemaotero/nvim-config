-- yanky.nvim: yank history; C-n/C-p cycle through previous yanks after paste; `<leader>sy` telescope picker
-- keywords: yank, paste, clipboard, register, history
return { "gbprod/yanky.nvim", config = true }
