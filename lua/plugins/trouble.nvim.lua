-- trouble.nvim: pretty list of diagnostics, references, quickfix, loclist (`<leader>i*` mappings)
-- keywords: diagnostics, errors, warnings, quickfix, loclist, lsp, references
return {
    "folke/trouble.nvim",
    opts = {},
    cmd = "Trouble",
}
